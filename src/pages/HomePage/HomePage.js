import React from 'react';
import ImageFinder from '../../components/ImageFinder'
const Home = (props) => {
    return (
        <div>
            <ImageFinder/>
        </div>
    );
};

export default Home;